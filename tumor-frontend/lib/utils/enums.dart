enum AlignTextType { left, center, right, justify }

enum EndPointName {
  // auth keywords
  profiency,
}

// Enum untuk tipe screen yang lebih type-safe
enum ScreenType { mobile, tablet, desktop }

enum SnackbarType { error, success, info, custom }

enum CalendarFormat { month, twoWeeks, week }
